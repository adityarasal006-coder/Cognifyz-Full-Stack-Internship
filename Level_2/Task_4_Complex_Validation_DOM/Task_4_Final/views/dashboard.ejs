<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Command Center | Task 4</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        :root { --cyan: #00F2FF; }

        body {
            background-color: #000;
            color: #fff;
            font-family: Consolas, monospace;
        }

        .elite-card {
            background: rgba(15, 15, 18, 0.95);
            border: 1px solid var(--cyan);
            border-radius: 12px;
            box-shadow: 0 0 40px rgba(0, 242, 255, 0.2);
        }

        .text-cyan { color: var(--cyan); }

        .stat-box {
            border: 1px solid #333;
            padding: 20px;
            border-radius: 8px;
            background: #0a0a0a;
            height: 100%;
        }

        .stat-box p {
            color: #FFFFFF !important;
            opacity: 1 !important;
            font-size: 1rem;
        }

        @keyframes slideIn {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .animate-up {
            animation: slideIn 0.8s cubic-bezier(0.2, 1, 0.2, 1);
        }
    </style>
</head>

<body>

<div class="container py-5">
    <div class="elite-card p-5 animate-up">

        <h1 class="display-4 fw-bold mb-4">
            SYSTEM <span class="text-cyan">ONLINE</span>
        </h1>

        <div class="mt-4 p-4 bg-dark rounded border-start border-info border-4">
            <h5 class="text-white">
                OPERATOR:
                <span class="text-cyan"><%= user.name %></span>
            </h5>

            <p class="text-white mb-0">
                Uplink established at <%= user.time %>.
            </p>
        </div>

        <a href="/" class="btn btn-outline-danger mt-5 fw-bold px-5">
            TERMINATE SESSION
        </a>

    </div>
</div>

</body>
</html>
