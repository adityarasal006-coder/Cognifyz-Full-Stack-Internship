<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Success!</title>
    <style>
        body { font-family: sans-serif; background: #f4f7f6; display: flex; align-items: center; justify-content: center; height: 100vh; margin: 0; }
        .success-card { background: white; padding: 30px; border-radius: 12px; box-shadow: 0 4px 15px rgba(0,0,0,0.1); text-align: center; border-top: 5px solid #28a745; }
        .data { color: #555; font-weight: bold; }
        a { display: inline-block; margin-top: 20px; color: #764ba2; text-decoration: none; font-weight: bold; }
    </style>
</head>
<body>
    <div class="success-card">
        <h2 style="color: #28a745;">✅ Submission Success!</h2>
        <p>Welcome, <span class="data"><%= name %></span></p>
        <p>Your email <span class="data"><%= email %></span> has been stored in temporary storage.</p>
        <a href="/">← Go Back</a>
    </div>
</body>
</html>